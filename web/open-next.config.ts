import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // Basic configuration for Cloudflare deployment
  // R2 incremental cache can be added later if needed
});
